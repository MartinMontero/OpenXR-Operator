# Shared context for every task card

Canonical design: docs/BUILD_KIT_v3.pdf (Part 6 wave plan).
Contracts (read-only for workers): docs/contracts/, operator/.../driver.py,
operator/.../schema.py, operator/.../model.py, addon/.../protocol.gd.
Standing rules: RPI; Rule 9; zero fabrication (CONFIRMED/INFERRED/BLOCKED/
NOT ATTEMPTED/UNTESTED); executable claims must be executed; licence split
invariant; vendor gate; security gates invariant; terse output.
